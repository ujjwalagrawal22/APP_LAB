#include <stdio.h>
#include <stdlib.h>
int main()
{
    //int c;
    // double nc;

    // for(nc=0;getchar()!=EOF;++nc);

    // printf("%.0f\n", nc);

    //c=getchar();

    // while(c!=EOF)
    // {
    //     putchar(c);
    //     c=getchar();
    // }

    // while((c=getchar())!=EOF) //optimised code for the above 
    // {
    //     putchar(c);
    //     c=getchar();
    // }

    //print EOF value

    //printf("The value of EOF is: %d\n", EOF);

    //  int c,n1;   // count number of new lines where input is taken

    //  n1=0;

    //  while((c=getchar())!=EOF)
    //  if(getchar()=='\n')
    //  ++n1;

    //  printf("%d",n1);


    
    return 0;
}